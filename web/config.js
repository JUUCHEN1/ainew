/* ============================================================
 * 漫创AI Web · 运行时配置 (Runtime config)
 * ------------------------------------------------------------
 * 这个文件在前端主程序加载之前执行，用来告诉前端"后端地址在哪里"。
 * 修改后无需重新构建，刷新浏览器即可生效。
 *
 * backendBaseUrl 规则：
 *   - 本地开发 / 同机使用：   "http://127.0.0.1:8765"
 *   - 部署到服务器（独立域名）："https://api.your-domain.com"
 *   - 前后端同域名反向代理：   写 window.location.origin（见下方注释）
 *
 * 注意：这里不能留空字符串。前端在取值为空时会回退到内置默认
 * (http://127.0.0.1:8765)，所以同源部署要显式写 location.origin。
 * ============================================================ */
(function () {
  // 后端 FastAPI 服务地址。默认指向本机 8765 端口。
  window.__LIBAI_BACKEND_BASE_URL__ = "http://127.0.0.1:8765";

  // —— 同源反向代理部署时改用下面这行（注释上面那行）——
  // window.__LIBAI_BACKEND_BASE_URL__ = window.location.origin;
})();
